#!/bin/sh

if [ $# -ne 1 ]; then
    echo "[Usage] $0 (target file)"
    exit
fi

if [ -f $1 ]; then
  cp $1 "${DESKTOP}"
elif [ -d $1 ]; then
  cp -r $1 "${DESKTOP}"/
else
  echo "Invalid request : $1"
fi
