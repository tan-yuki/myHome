#!/bin/sh

if [ $# -eq 0 ];then
  echo "[Usage] `basename $0` (command)"
  exit 0
fi

# get commnad from arguments
command=""
for arg in $@; do
  command="${command} ${arg}"
done

# execute
#nohup "${command}" &
nohup "$command"  > /dev/null 2>&1 &

exit 0
